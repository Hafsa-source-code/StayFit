@model IEnumerable<StayFit.Models.Domain.Workout>

<div class="card shadow p-4 mb-4">
    <h3 class="fw-bold text-danger text-center mb-3">
        🔥 Weight Loss Workout Plan
    </h3>

    <p class="text-muted text-center mb-3">
        Follow this routine to burn fat effectively
    </p>

    <ul class="list-group fw-bold">
        @if (Model != null && Model.Any())
        {
            foreach (var workout in Model)
            {
                <li class="list-group-item">
                    @workout.Description
                </li>
            }
        }
        else
        {
            <li class="list-group-item text-muted">No workout plan available.</li>
        }
    </ul>
</div>
